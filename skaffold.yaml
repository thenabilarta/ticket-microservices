apiVersion: skaffold/v3
kind: Config
build:
  local:
    push: false
  artifacts:
    - image: mnabilarta/ticket-auth
      context: ticket-auth
      sync:
        manual:
          - src: src/**/*.ts
            dest: .
      docker:
        dockerfile: Dockerfile
manifests:
  rawYaml:
    - ./infra/k8s/*
deploy:
  kubectl: {}
